export enum DrawerNavigation {
  START = 'Start',
}

export enum TabsNavigation {
  HOME = 'Home',
  CONTACT = 'Contact',
}

export enum MainNavigation {
  PRODUCTS = 'Products',
  PRODUCT = 'Product',
}
